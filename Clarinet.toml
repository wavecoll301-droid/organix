[project]
name = 'organix'
description = ''
authors = []
telemetry = true
cache_dir = './.cache'
requirements = []
[contracts.authority-registry]
path = 'contracts/authority-registry.clar'
clarity_version = 3
epoch = 'latest'

[contracts.certification-nft]
path = 'contracts/certification-nft.clar'
clarity_version = 3
epoch = '3.2'
[repl.analysis]
passes = ['check_checker']

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false

[repl.remote_data]
enabled = false
api_url = 'https://api.hiro.so'
